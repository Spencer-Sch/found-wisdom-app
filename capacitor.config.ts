import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'found-wisdom',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
